#include "handler.c"

/* Encrypt a given payload with the session key */
int tf_encrypt();

/* Decrypt a payload with the session key */
int tf_decrypt();

/* Public KKey and Private Key exchange */
int tf_key_exchange();
